@model IEnumerable<SYSTEM_MANAGEMENT.Models.LIST_PERMISSIONS>

@{
    ViewBag.Title = "Danh sách quyền";
}

<h2>Danh Sách Quyền</h2>
<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.PERMISSION_NAME)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DESCRIPTION)
        </th>
        
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.PERMISSION_NAME)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DESCRIPTION)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PERMISSION_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.PERMISSION_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PERMISSION_ID })
        </td>
    </tr>
}

</table>
